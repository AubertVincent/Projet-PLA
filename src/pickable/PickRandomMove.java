package pickable;

import carte.Coordinates;
import carte.Map;

public class PickRandomMove extends PickAble {

	public PickRandomMove(Coordinates coord, Map entityMap) {
		super(coord, entityMap);
	}

}
