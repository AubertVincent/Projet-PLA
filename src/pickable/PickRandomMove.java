package pickable;

import carte.Coordinates;
import carte.Map;

public class PickRandomMove extends PickAble {

	public PickRandomMove(Coordinates coordinates, Map entityMap) {
		super(coordinates, entityMap);
	}

}
